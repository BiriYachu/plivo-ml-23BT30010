"# Implementation Plan - LLM Speedrun\n\nWe aim to improve the mediocre GPT baseline model under a hard budget of <= 2000 steps and <= 2M parameters.\n\n## User Review Required\n\n> [!IMPORTANT]\n> The final model must have <= 2,000,000 parameters and <= 2000 optimizer training steps. We propose to use a BPE tokenizer with a vocabulary size of 1024, which significantly compresses Hindi characters (reducing token count by ~2.2x) and saves param budget.\n\n## Questionable Design Choices in Baseline\n1. **Raw Byte Tokenizer**: Treating Devanagari characters as 3 separate tokens drastically inflates sequence length. This wastes context length (128) and increases the number of steps required to train on the text.\n2. **No Weight Tying**: The input embeddings and output projection head are separate, wasting parameter capacity.\n3. **No Learning Rate Schedule**: Constant LR of 3e-4 is sub-optimal. Cosine decay with warmup is much better.\n4. **Poor Initialization**: Initialization is a standard normal with `std=0.05` for all layers. Standard practice is to scale the residual block output projection weights by `1 / sqrt(2 * n_layer)`.\n5. **No Gradient Clipping**: Training runs without gradient clipping, making it vulnerable to gradient spikes.\n6. **No Position Embedding Optimization**: Learned absolute position embeddings are parameter-inefficient and don't generalize as well as Rotary Position Embeddings (RoPE).\n7. **Optimizer Hyperparameters**: Standard Adam without weight decay instead of AdamW.\n8. **No Dropout Tuning**: Dropout is set to 0.0, which might lead to overfitting.\n\n## Proposed Changes\n\n### Tokenizer\n\n#### [MODIFY] [tokenizer.py](file:///C:/Users/biriy/.gemini/antigravity/scratch/llm_speedrun/llm_handout/starter/tokenizer.py)\nWe will implement a custom Byte-level Byte Pair Encoding (BPE) tokenizer trained on the train corpus.\n- **Vocab Size**: 1024 (256 bytes + 768 merge tokens). This limits the embedding parameter cost while achieving high compression on Hindi.\n- **Lossless guarantee**: Every 
<truncated 2518 bytes>